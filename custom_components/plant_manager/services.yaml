mark_watered:
  name: Mark watered
  description: Mark a plant as watered.
  fields:
    plant_id:
      required: true
      example: "1234abcd-5678-..."
      selector:
        text:

send_digest_now:
  name: Send digest now
  description: Send the plant watering digest notification immediately.
  fields: {}
